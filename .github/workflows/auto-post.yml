name: Auto Post

on:
  workflow_dispatch:
  schedule:
    - cron: "20 1 * * *" # 매일 01:20 UTC (KST 10:20)

permissions:
  contents: write

concurrency:
  group: auto-post
  cancel-in-progress: true

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate post (HTML + Unsplash only)
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          SITE_NAME: MingMong
          SITE_URL: https://mingmonglife.com
          POSTS_PER_RUN: 1
          MIN_CHARS: 2800
          MODEL: gpt-4o-mini
          UNSPLASH_MIN_WIDTH: 2000
          UNSPLASH_MIN_HEIGHT: 1200
          UNSPLASH_MIN_LIKES: 60
          UNSPLASH_PER_PAGE: 30
          MAX_KEYWORD_TRIES: 12
        run: |
          python tools/auto_post.py

      - name: Build sitemap
        env:
          SITE_URL: https://mingmonglife.com
        run: |
          python tools/build.py

      - name: Commit and push
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Auto post"
          git pull --rebase origin main
          git push origin HEAD:main
